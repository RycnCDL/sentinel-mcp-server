# Azure Authentication
# =====================
# Choose one authentication method:

# Option 1: Service Principal (Recommended for Production)
AZURE_TENANT_ID=your-tenant-id-here
AZURE_CLIENT_ID=your-client-id-here
AZURE_CLIENT_SECRET=your-client-secret-here

# Option 2: Managed Identity (for Azure-hosted deployments)
# AZURE_USE_MANAGED_IDENTITY=true

# Option 3: Azure CLI (for local development)
# Will use `az login` credentials automatically if above are not set

# Sentinel Configuration
# =======================
# Default subscription (optional - can be specified per workspace)
AZURE_SUBSCRIPTION_ID=your-default-subscription-id

# Logging Configuration
# ======================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=json  # json or text

# MCP Server Configuration
# =========================
MCP_SERVER_NAME=sentinel-mcp-server
MCP_SERVER_VERSION=0.1.0-alpha

# Performance & Limits
# ====================
# Maximum concurrent workspace queries
MAX_CONCURRENT_QUERIES=5

# Query timeout in seconds
QUERY_TIMEOUT_SECONDS=30

# KQL result limit per workspace
KQL_RESULT_LIMIT=1000

# Cache Configuration
# ===================
# Enable caching for workspace lists
ENABLE_WORKSPACE_CACHE=true

# Cache TTL in seconds
WORKSPACE_CACHE_TTL=300

# Development Settings
# ====================
# Enable debug mode
DEBUG_MODE=false

# Enable request logging
LOG_REQUESTS=true
